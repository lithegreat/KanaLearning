<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="KanaLearning.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="KanaLearning"
    mc:Ignorable="d">
    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TitleBar x:Name="AppTitleBar" Title="KanaLearning">
            <TitleBar.IconSource>
                <ImageIconSource ImageSource="Assets/AppIcon.ico" />
            </TitleBar.IconSource>
        </TitleBar>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="8" Padding="12,8">
            <Button x:Name="QuizNavButton" Click="OnQuizNavButtonClick" />
            <Button x:Name="SettingsNavButton" Click="OnSettingsNavButtonClick" />
        </StackPanel>

        <Frame x:Name="ContentFrame" Grid.Row="2" />
    </Grid>
</Window>
